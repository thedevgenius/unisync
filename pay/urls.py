from django.urls import path
# from .views import FeesAddView, FeesEditView

urlpatterns = [
    # path('fees/add/', FeesAddView.as_view(), name='fees_add'),
    # path('fees/edit/<int:id>/', FeesEditView.as_view(), name='fees_edit'),
]
